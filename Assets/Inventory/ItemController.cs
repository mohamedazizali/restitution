using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemController : MonoBehaviour
{
    public Item item; // Référence à l'objet Item contrôlé par ce script

    // Start est appelé avant la première mise à jour du frame
    void Start()
    {
        // Initialisation ou configuration de l'objet Item peut être faite ici
    }

    // Update est appelé une fois par frame
    void Update()
    {
        // Logique à chaque frame, par exemple, pour les interactions ou mises à jour dynamiques
    }
}
